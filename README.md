# weather
weather via golang

## Usage
### Run the Application
Change to the weather.go source directory then execute the file.

```
go run weather.go
```

Then either curl or navigate in a browser to weather endpoint supplying the city name that you want the weather for.


```
curl http://localhost:8080/weather/denver
```

